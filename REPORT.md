# Отчет по домашнему заданию ЛР8

## Источник задания

Лабораторная работа выполнена по репозиторию `tp-lessons/lab_docker`.

## Задание

В домашнем задании требуется подготовить web-приложение, которое сохраняет введенную задачу в базу данных, добавить Dockerfile и настроить `docker-compose.yml` для совместной работы приложения и MySQL.

## Что сделано

1. Добавлен каталог `app/` с Flask-приложением.
2. Добавлен файл `requirements.txt` с зависимостями.
3. Добавлен каталог `db/` и файл `db/init.sql` для создания таблицы `tasks`.
4. Обновлен `Dockerfile` для запуска Python-приложения.
5. Добавлен `docker-compose.yml` с сервисами `app` и `db`.
6. Для приложения открыт порт `5000`.
7. Для MySQL добавлен healthcheck и volume.

## Команды проверки Docker

```sh
docker build -t lab-docker .
docker run --rm -it -p 5000:5000 lab-docker
```

## Команды проверки Docker Compose

```sh
docker compose up --build
```

После запуска приложение должно быть доступно в браузере по адресу:

```text
http://localhost:5000
```

## Проверка работы приложения

1. Открыть `http://localhost:5000`.
2. Ввести название задачи.
3. Нажать кнопку добавления.
4. Убедиться, что задача появилась в списке.

## Вывод

Домашнее задание выполнено: web-приложение запускается в Docker-контейнере, а через Docker Compose поднимается связка приложения и базы данных MySQL.

## Ссылка

https://github.com/NorthDakota11/lab8
